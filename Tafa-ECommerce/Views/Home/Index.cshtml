@model HomeViewModel

@await Html.PartialAsync("Carousel");

<h2>Here are all the preferred clothes</h2>
<h4>Clients preferences -just for you</h4>

<div class="row">
    @foreach(var clothes in Model.PreferredClothes)
    {
        @Html.Partial("ClothesSummary", clothes);
    }
</div>